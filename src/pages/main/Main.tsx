import Loader from 'components/Loader/Loader';
import React from 'react';
import type { FC } from 'react';

const Main: FC = () => {
  return (
    <div>
      Main
      <Loader />
    </div>
  );
};

export default Main;
